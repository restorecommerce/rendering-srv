{
  "name": "@restorecommerce/rendering-srv",
  "version": "0.0.8",
  "description": "Handlebars-HTML rendering microservice",
  "main": "index.js",
  "author": "Invend GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/rendering-srv.git"
  },
  "license": "MIT",
  "keywords": [
    "microservice",
    "rendering"
  ],
  "dependencies": {
    "@restorecommerce/chassis-srv": "github:restorecommerce/chassis-srv",
    "@restorecommerce/cluster-service": "^0.1.1",
    "@restorecommerce/handlebars-helperized": "github:restorecommerce/handlebars-helperized",
    "@restorecommerce/kafka-client": "github:restorecommerce/kafka-client",
    "@restorecommerce/logger": "^0.1.6",
    "@restorecommerce/protos": "github:restorecommerce/protos",
    "@restorecommerce/service-config": "^0.1.1",
    "cheerio": "^1.0.0-rc.2",
    "lodash": "^4.17.10",
    "node-fetch": "^2.2.0",
    "tslint": "^5.13.0",
    "typescript": "^3.3.3333"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.6",
    "cross-env": "^5.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^6.0.2",
    "nodemon": "^1.18.10",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.3",
    "should": "^13.2.3"
  },
  "scripts": {
    "start": "npm run createtopics && node start.js",
    "dev": "cross-env NODE_ENV=development npm run createtopics && nodemon start.js --trace-warnings",
    "pretest": "npm run tsctests && npm run createtopics",
    "test": "npm run lint && npm run mocha",
    "lint": "tslint ./lib/*.ts ./test*.ts ./service.ts ./start.ts",
    "mocha": "cross-env NODE_ENV=test mocha --timeout 60000 ./test/test.js --stack-trace --exit",
    "coveralls": "cross-env NODE_ENV=test istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec ./test/*.js --exit",
    "postinstall": "tsc -d",
    "createtopics": "node setupTopics.js io.restorecommerce.rendering",
    "test-debug": "cross-env NODE_ENV=test mocha --inspect-brk",
    "test-prod-env": "cross-env NODE_ENV=prod mocha",
    "tsctests": "tsc ./test/test.ts --module commonjs -target ES6",
    "build:tsc": "tsc -d",
    "build:clean": "rimraf service.js start.js",
    "build": "npm-run-all lint build:clean build:tsc"
  },
  "engines": {
    "node": ">= 9.2.0"
  }
}
